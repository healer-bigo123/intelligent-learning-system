# 智能学习系统 · 协作规范与分工通知

> 发送人：A（后端负责人）
> 接收人：B（前端）、C（AI/算法+测试）
> 日期：2026-07-12

---

各位，Git 仓库已调整完毕，`develop` 分支已推送到 GitHub。后续按以下规范协作：

## 一、分支规范（立即执行）

```
main                              ← 稳定版本，只在 Day 15 最终汇总时合并，平时不要动
develop                           ← 日常集成分支，每晚三人代码合并到这里
feature/你的名字-功能名            ← 每人各自的功能分支
```

**你每次开工的操作：**

```powershell
git checkout develop
git pull origin develop
git checkout -b feature/你的名字-今日任务
# 开发...
git add .
git commit -m "feat: 完成xxx"
git push origin feature/你的名字-今日任务
# 然后在 GitHub 上提 PR：feature/你的名字-今日任务 → develop
```

**禁止操作：**

- 不要直接 push 到 `main`
- 不要直接 push 到 `develop`（必须通过 PR）
- 不要改别人的代码文件

---

## 二、B（前端）具体工作安排

根据分工计划，你需要按以下节奏推进：

| 时间 | 你要做的 | 分支名建议 | 对接的后端接口 |
|------|---------|-----------|--------------|
| Day 5-6 | 新增「错题本」「练习测试」页面 | `feature/B-mistakes-page` | `/api/v1/mistakes/`、`/api/v1/exercises/` |
| Day 7-8 | 对接 `immersive-agent.vue`，替换 mock | `feature/B-agent-integration` | `/api/v1/chat/`、`/api/v1/agents/` |
| Day 9 | 新增「思维导图」「学习路径」页面 | `feature/B-mindmap-path` | `/api/v1/mindmaps/`、`/api/v1/learning-paths/` |
| Day 10 | 新增「课堂」「通知中心」「成就墙」页面 | `feature/B-classroom-notify` | `/api/v1/classroom/`、`/api/v1/notifications/`、`/api/v1/achievements/` |
| Day 11 | `Profile.vue` 对接真实 API，替换 mock | `feature/B-profile-api` | `/api/v1/image-recognition/`、`/api/v1/external-data/` |
| Day 12 | 新增「学习网站」页；前端 API 层模块化封装 | `feature/B-websites-refactor` | `/api/v1/learning-websites/` |
| Day 13 | 前端 bug 修复、UI 打磨、响应式适配 | `feature/B-bugfix` | — |
| Day 14 | 前端构建优化、打包、生产环境配置 | `feature/B-build-optimize` | — |

**关键提醒：**

- 对接前先查 Swagger 文档：启动后端后访问 `http://localhost:8000/docs`
- 不要按想象调接口，字段名、请求方式以 Swagger 为准
- 前端 API 层目前只有 `agent.ts` 和 `client.ts`，Day 12 需要做模块化封装（每个模块一个 ts 文件）
- `Profile.vue` 和 `immersive-agent.vue` 目前用的是 mock 数据，需要替换为真实 API 调用

---

## 三、C（AI/算法+测试）具体工作安排

| 时间 | 你要做的 | 分支名建议 | 涉及的后端文件 |
|------|---------|-----------|--------------|
| Day 5-6 | 编写错题/练习接口测试用例 | `feature/C-mistakes-tests` | `backend/tests/` |
| Day 7-8 | 完善向量库、RAG 检索、LLM 客户端 | `feature/C-rag-llm` | `backend/app/core/vector_store.py`、`knowledge_base.py`、`llm_client.py` |
| Day 9 | 知识库文档向量化、路径推荐算法 | `feature/C-knowledge-vector` | `backend/app/core/knowledge_base.py` |
| Day 10 | 模块集成测试 | `feature/C-integration-tests` | `backend/tests/` |
| Day 11 | 图片识别功能测试、画像分析算法 | `feature/C-image-tests` | `backend/app/api/v1/endpoints/image_recognition.py` |
| Day 12 | 端到端联调、缺口清单 | `feature/C-e2e-check` | 全量 |
| Day 13 | 端到端测试、性能压测、缺陷跟踪 | `feature/C-perf-tests` | 全量 |
| Day 14 | 编写部署文档、用户手册、API 文档 | `feature/C-docs` | `docs/` |

**关键提醒：**

- 你之前上传的 6 个文件（意图关键词扩展、Windows 路径修复、finally 清理、依赖补充、LLM 稳定性测试）我已经看到，质量不错，会合并到 develop
- 发现的「多轮对话记忆缺失」问题放到 Day 7-8 一起修
- 测试用例放在 `backend/tests/` 目录下，用 pytest 运行
- 文档放在 `docs/` 目录下

---

## 四、最终整合流程（Day 15）

```
第 1 步：三人的 feature 分支全部合并到 develop（通过 PR）
         ↓
第 2 步：在 develop 上跑全量回归测试
         ↓
第 3 步：develop 合并到 main
         git checkout main
         git pull origin main
         git merge develop
         git push origin main
         ↓
第 4 步：A 统一跑一次 python init_data.py 重置数据库
         ↓
第 5 步：打 tag v1.0，准备演示
```

**整合时的冲突解决原则：**

| 冲突类型 | 解决人 |
|---------|--------|
| 前端文件冲突 | B 解决 |
| 后端文件冲突 | A 解决 |
| 测试/文档冲突 | C 解决 |
| 接口定义冲突 | 以 Swagger 为准 |

---

## 五、每晚同步流程

1. 各自 push 自己的 feature 分支
2. 在 GitHub 上提 PR 到 develop
3. 另外两人 review 后合并
4. 各自 `git pull origin develop` 拉取最新代码
5. 对齐次日任务

---

## 六、接口契约

- 后端 FastAPI 自动生成 Swagger：`http://localhost:8000/docs`
- 前端依据 Swagger 对接，避免口头约定
- 接口变更必须通知其他人，不要私自改字段

## 七、数据库同步

- SQLite 无法多人共享，每人本地各自运行 `python init_data.py` 初始化
- schema 变更通过 `migrate.py` 脚本同步
- **不要直接改本地 db 文件**

## 八、风险提示

- **SQLite 并发**：最终汇总时只能用一份 db，建议 Day 15 指定 A 的环境为基准，B/C 的数据通过 SQL 导出关键表合并
- **LLM 密钥**：三人各自 `.env` 配置，**不要提交到 Git**（确认 `.env` 已在 `.gitignore`）
- **接口不匹配**：B 对接前先查 Swagger，避免按想象调接口

---

有问题随时沟通。
